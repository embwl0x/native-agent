No edits or builds performed.

| Row | Key | Write path same as original? | Consumer | Live or relaunch | Round-trip | Verdict |
|---|---|---|---|---|---|---|
| Reflection between conversations | Reads/writes `UserDefaults["cognitiveSubstrateReflectionEnabled"]` at `SetupFeatureRows.swift:22,78-84`. Effective value is master-gated at `NativeCognitionRuntime.swift:2302-2313`. | **Yes.** Both call `NativeCognitionRuntime.setReflectionEnabled`, `SetupFeatureRows.swift:81-84` versus `CognitionObservatoryView.swift:470-474`; setter persists and reconfigures at `NativeCognitionRuntime.swift:1894-1898`. **Read divergence:** the original rereads effective runtime state at `CognitionObservatoryView.swift:523-528`; Setup displays the requested `@AppStorage` bit. | CognitiveSubstrate reflection admission, including `planReflectionChecked`; explicit gate at `NativeCognitionRuntime+Reflection.swift:78-97,213-227`. Provider-routing failure can force the effective flag off at `NativeCognitionRuntime.swift:1124-1129`. | **Live for subsequent reflection admission**, through `refreshConfiguration()` → `substrate.configure`, `NativeCognitionRuntime.swift:1108-1138`; an already-running reflection is not cancelled. | Direct off/on restores the Boolean and preserves the budget. **Not exact through the master:** `setSubconsciousMasterEnabled` overwrites the flag and budget, `NativeCognitionRuntime.swift:1914-1920`. | **MISLEADING** — it can show On while effective reflection is off because of routing or budget, and the new card does not disclose the master’s destructive reset. `SetupFeatureRows.swift:72-87`; original disclosure: `CognitionObservatoryView.swift:182-187`. |
| Moods, energy, and a clock | Reads/writes `UserDefaults["organismKernelEnabled"]`, `SetupFeatureRows.swift:23,98-104`. | **Yes.** Same `setOrganismKernelEnabled` call as `CognitionObservatoryView.swift:484-488`; implementation at `NativeCognitionRuntime+Organism.swift:302-313`. **Read divergence:** original uses `next.organism.enabled`, `CognitionObservatoryView.swift:527`; Setup uses the stored preference. | `OrganismKernel`; disabled kernels reject ingestion and project neutral state, `OrganismKernel.swift:81-94,523-571`. Public-safe startup may force the kernel neutral, `NativeCognitionRuntime.swift:2347-2363`. | **Live.** The existing kernel and somatic bus are reconfigured immediately at `NativeCognitionRuntime.swift:1131-1138`; enabling also restores continuity and refreshes the body schema. | Direct off/on restores the Boolean. **Not exact through the master:** the master writes this key false/true, `NativeCognitionRuntime.swift:1915-1920`. | **MISLEADING** — dependency disabling is correct at `SetupFeatureRows.swift:94-107`, but displayed state is requested storage rather than effective kernel state, and the master’s reset behavior is omitted. |
| Memory in every reply | `UserDefaults["contextFlowMode"]`, with `off`, `shadow`, or `active`, `SetupFeatureRows.swift:24,119-128`. | **Yes.** Both controls persist through `NativeContextFlowRuntime.setMode`, `SetupFeatureRows.swift:139-147` versus `SlimSettingsView.swift:934-948`; owner write at `NativeContextFlowRuntime.swift:520-530`. | `NativeContextFlowRuntime`/`ContextFlowCoordinator`; Off does not start the coordinator, `ContextFlowCoordinator.swift:278-295`; Observe Only prepares packets detached without supplying them, while Active supplies the prepared packet, `ChatOrchestration+TurnEngine.swift:1298-1315`. | **Live for the next turn.** `setMode` stops and reconstructs the coordinator, `NativeContextFlowRuntime.swift:495-530`; an already-prepared turn retains its existing lease. | Direct three-way selection round-trips exactly. **Not exact through the master:** enabling “An inner life” forces Active, while disabling does not restore the previous mode, `SetupView.swift:566-597`. The card does state that enabling turns memory in every reply on, `SetupView.swift:327-330`. | **MISLEADING** — the picker displays the stored request even when onboarding or `NATIVE_AGENT_CONTEXT_FLOW_MODE` forces another effective mode, `NativeContextFlowRuntime.swift:297-316`; unlike the original’s persistent effective-state text at `SlimSettingsView.swift:950-963`, Setup emits only a transient toast. |
| Dreams at night | Composite: `personalityPolicy.dream_cycle_enabled && trainingPolicy.dream_scheduler`; read at `SetupFeatureRows.swift:487-490`, written together at `NativeClient+DreamActions.swift:254-260`. | **Yes.** Same `appModel.setDreamCycleEnabled` as `DreamsView.swift:334-351`; AppModel path at `AppModel+PersonalitySelfImprovement.swift:392-405`. | `DreamREMGatePolicy.dreamEnabled`, `DreamREMCycle.swift:190-216`; checked by each dream execution at `DreamREMCycle.swift:548-567`, including the scheduled job and pressure-dream lane. | **Live for the next dream attempt** because each new cycle reads Trust policy; it does not cancel an in-flight dream or remove the scheduled job. | **No.** The Boolean collapses four two-key states. Starting from an asymmetric state such as `(dream_cycle_enabled: true, dream_scheduler: false)`, On then Off ends at `(false,false)`, not the prior state. | **BROKEN** — exact round-trip fails. It can also go stale while mounted: `syncFromTrustPolicy()` updates REM but never recomputes `dreamCycleOn`, `SetupFeatureRows.swift:67,494-501`; only initial load and this row’s own save reread the composite. |
| Weekly dream consolidation | `trainingPolicy.rem_cycle_enabled`, read at `SetupFeatureRows.swift:217-219`, written at `SetupFeatureRows.swift:195-203`. | **Yes.** Same `appModel.setRemCycleEnabled` as `DreamsView.swift:358-370`; deep-merged write at `NativeClient+DreamActions.swift:262-267`. | Production consumer is the `nativeagent-weekly-rem` TriggerScheduler job → `SchedulerDueJobRunner.executeREM` → `NativeClient.runRem`, `BackgroundLoopsAssembly+DreamsMemory.swift:23-33` and `SchedulerDueJobRunner+Execution.swift:344-345`; the gate is enforced at `DreamREMCycle.swift:591-610`. `REMCycleLoop` is explicitly retired from production, `REMCycleLoop.swift:14-23`. | **Live for the next manual or scheduled REM invocation.** The weekly job remains scheduled and skips when gated off. | **Yes.** One Boolean is deep-merged and read back from the refreshed Trust policy, `SetupFeatureRows.swift:202-219,494-500`. | **WORKS** — same authority path, correct readback, no relaunch. |
| Memory mode | `data/config/embeddings/mode.json` field `mode`, normalized to `performance`, `balanced`, or `low_memory`, `ManagedEmbeddingProvider.swift` equivalent at `MemoryV2+EmbeddingRuntime.swift:267-288,423-442`. | **Yes.** Same `appModel.setEmbeddingsMemoryMode` as `SlimSettingsView.swift:1491-1501,1628-1641`; app/client path at `AppModel+HealthEmbeddings.swift:72-75` and `NativeClient+Providers.swift:1112-1119`. | `ManagedEmbeddingProvider`: Fast has no idle unload, Balanced 300 seconds, Low 45 seconds; Low also uses CPU-only Core ML, `MemoryV2+EmbeddingRuntime.swift:258-288,434-442`. | **Live for retention and the next embedding use.** Crossing into/out of Low unloads a resident model immediately so the next use reloads with the correct compute units. Selecting Fast while currently cold does not eagerly load it; eager Fast warm-up is launch-only at `NativeAgentEmbeddingWarmup.swift:35-60`, so it warms on next use or relaunch. | **Yes.** All three menu values persist and reread exactly after the setter, `SetupFeatureRows.swift:430-465`. | **WORKS** — initial state comes from the real runtime snapshot through `refreshEmbeddings()`, `SetupFeatureRows.swift:479-490,504-509`, and the setter replaces it with the returned post-write snapshot. |

### Ranked defects

1. **Dream composite destroys state:** preserve the pre-toggle two-key tuple and restore it when returning Off, or expose the two independent gates instead of claiming a reversible Boolean.

2. **Dream display is not synchronized:** make `syncFromTrustPolicy()` recompute `dreamCycleOn` from both policy fields whenever `appModel.trustPolicy` changes, while respecting `savingDream`.

3. **The inner-life master destroys subordinate choices:** change it to gate cognition without rewriting reflection, organism, and budget preferences, or explicitly state on the card that both directions reset all subordinate controls.

4. **Reflection and organism show requested, not effective state:** load and subscribe to `NativeCognitionRuntime.subconsciousRuntimeState()` as the original Observatory does, with requested/effective status shown separately when they differ.

5. **Context Flow hides effective mode:** retain `NativeContextFlowModeStatus` in Setup and show the original control’s persistent “forced Off/environment managed/effective mode” text instead of relying on a one-shot toast.