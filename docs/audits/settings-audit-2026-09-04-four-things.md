Read-only audit completed. No files changed; no build or tests run.

“Exact round-trip” below means both the persisted configuration and any durable side effects return to their prior state.

| Control | Reads | Writes (fn) | Consumer that honours it | Round-trips? | Hidden effects / state source | Verdict |
|---|---|---|---|---|---|---|
| Header | `appModel.agentDisplayName`, derived from cached `personality.name` or `chatPersona` ([SetupView.swift:312](Sources/NativeAgentApp/SetupView.swift:312), [AppModel.swift:687](Sources/NativeAgentApp/AppModel.swift:687)); copy uses `AgentVoice` ([SetupView.swift:223](Sources/NativeAgentApp/SetupView.swift:223)). | None. | Setup renders those values directly ([SetupView.swift:314](Sources/NativeAgentApp/SetupView.swift:314)). | N/A. | Display-only. It uses the `AppModel` projection, not a direct persona-store read; the Settings refresh does not reload personality ([AppModel+ChatSessions.swift:266](Sources/NativeAgentApp/AppModel+ChatSessions.swift:266)). | **WORKS**, but may carry a cached name ([AppModel.swift:687](Sources/NativeAgentApp/AppModel.swift:687)). |
| An inner life | Visible state reads `cognitiveSubstrateEnabled`; the setter also reads `cognitiveSubstrateDailyReflectionBudget` ([SetupView.swift:198](Sources/NativeAgentApp/SetupView.swift:198), [SetupView.swift:570](Sources/NativeAgentApp/SetupView.swift:570)). | `setInnerLife` → `setSubconsciousMasterEnabled`; writes master, capsule, background, reflection, reflection budget, and organism flags ([SetupView.swift:576](Sources/NativeAgentApp/SetupView.swift:576), [NativeCognitionRuntime.swift:1910](Sources/NativeAgentApp/NativeCognitionRuntime.swift:1910)). Turning on additionally calls `NativeContextFlowRuntime.setMode(.active)` and writes `contextFlowMode=active` ([SetupView.swift:591](Sources/NativeAgentApp/SetupView.swift:591), [NativeContextFlowRuntime.swift:524](Sources/NativeAgentApp/NativeContextFlowRuntime.swift:524)). | Cognition builds its effective configuration from `cognitiveSubstrateEnabled` and the subordinate keys ([NativeCognitionRuntime.swift:2297](Sources/NativeAgentApp/NativeCognitionRuntime.swift:2297)); chat prepares a cognitive projection ([ChatOrchestrationClient+StructuredChat.swift:1245](Modules/NativeAgentCore/Sources/ChatOrchestration/ChatOrchestrationClient+StructuredChat.swift:1245)); capsule generation checks master plus capsule flag ([CognitiveSubstrate+Capsule.swift:8](Modules/NativeAgentCore/Sources/CognitiveSubstrate/CognitiveSubstrate+Capsule.swift:8)). Context-backed memory separately reads `contextFlowMode` ([NativeContextFlowRuntime.swift:613](Sources/NativeAgentApp/NativeContextFlowRuntime.swift:613)). | **No.** On→off overwrites all subordinate settings with false/zero and leaves Context Flow active. It also does not remove cognition/organism state created while enabled. | Enabling may bootstrap cognition, restore/persist organism continuity, reschedule deadlines, and publish invalidations ([NativeCognitionRuntime.swift:1922](Sources/NativeAgentApp/NativeCognitionRuntime.swift:1922)). The switch is store-backed via `@AppStorage`, but its claim about “memory in every reply” is controlled by a different key which off does not undo. | **MISLEADING** — off does not turn off the card’s advertised “memory in every reply,” and the control destructively rewrites unrelated lane choices ([SetupView.swift:566](Sources/NativeAgentApp/SetupView.swift:566)). |
| Moments the agent keeps | `momentsLaneEnabled`, default true ([SetupPresentation.swift:13](Sources/NativeAgentApp/SetupPresentation.swift:13), [SetupView.swift:207](Sources/NativeAgentApp/SetupView.swift:207)). | Direct `@AppStorage` assignment, not `MomentsLaneSetting.setEnabled`; also changes `appModel.statusText` ([SetupView.swift:350](Sources/NativeAgentApp/SetupView.swift:350)). | Launch injects `MomentsLaneSetting.isEnabled()` as a fresh-per-turn closure ([AppDelegate+Launch.swift:322](Sources/NativeAgentApp/AppDelegate+Launch.swift:322)); the promoter checks it before extraction or quota reservation ([MemoryV2+AdaptivePromoter.swift:581](Modules/NativeAgentCore/Sources/MemoryV2/MemoryV2+AdaptivePromoter.swift:581)). | Preference: **yes**. Any moment staged while briefly enabled remains, correctly, in memory. | Only transient status text. State is directly `@AppStorage`-backed and cannot drift from that defaults key. | **WORKS** ([SetupView.swift:347](Sources/NativeAgentApp/SetupView.swift:347)). |
| The agent’s hour | `StudioWanderLane.enabledDefaultsKey` = `studioWanderEnabled`; also visually depends on `cognitiveSubstrateEnabled` ([StudioWanderLane.swift:53](Modules/NativeAgentCore/Sources/BackgroundLoops/StudioWanderLane.swift:53), [SetupView.swift:210](Sources/NativeAgentApp/SetupView.swift:210)). | Direct `@AppStorage` assignment, then `NativeCognitionRuntime.reloadStudioWanderInstallation()` ([SetupView.swift:364](Sources/NativeAgentApp/SetupView.swift:364)). | Installation reads the same key plus the subconscious master ([BackgroundLoopsAssembly+Studio.swift:205](Sources/NativeAgentApp/BackgroundLoopsAssembly+Studio.swift:205)); the residual lane refuses before reading state unless installed ([BackgroundLoopsAssembly+Studio.swift:152](Sources/NativeAgentApp/BackgroundLoopsAssembly+Studio.swift:152)). | Preference: **yes**. A wander trace/refractory generated while enabled remains. | Invalidates the cached installation ([BackgroundLoopsAssembly+Studio.swift:668](Sources/NativeAgentApp/BackgroundLoopsAssembly+Studio.swift:668)). State is real `@AppStorage`, and execution rechecks current eligibility at tool boundaries ([BackgroundLoopsAssembly+Studio.swift:409](Sources/NativeAgentApp/BackgroundLoopsAssembly+Studio.swift:409)). | **WORKS** ([SetupView.swift:361](Sources/NativeAgentApp/SetupView.swift:361)). |
| Use my Mac | A one-time local `@State` copy of `MacIntegrationPermissionStore.current()`; “on” means any read or write bit is true ([SetupView.swift:216](Sources/NativeAgentApp/SetupView.swift:216), [SetupView.swift:400](Sources/NativeAgentApp/SetupView.swift:400), [SetupView.swift:608](Sources/NativeAgentApp/SetupView.swift:608)). | **Nothing.** The binding is `.constant(anyMacCapabilityGranted)` and clicking the switch navigates to `.macIntegration` ([SetupView.swift:377](Sources/NativeAgentApp/SetupView.swift:377), [SetupView.swift:744](Sources/NativeAgentApp/SetupView.swift:744)). `denyEveryMacCapability` exists but has no caller ([SetupView.swift:613](Sources/NativeAgentApp/SetupView.swift:613)). | Actual tools call `MacIntegrationPermissionStore.allows` before dispatch ([SwiftToolDispatcher+MacIntegration.swift:29](Modules/NativeAgentCore/Sources/ChatOrchestration/SwiftToolDispatcher+MacIntegration.swift:29)); that gate reads the disk store and denies corrupt state ([MacIntegrationPermissions.swift:259](Modules/NativeAgentCore/Sources/MacIntegration/MacIntegrationPermissions.swift:259)). | **No flip occurs.** Clicking on or off only opens another page. | `current()` converts corrupt/unreadable authority into deny-all without exposing an error ([MacIntegrationPermissions.swift:243](Modules/NativeAgentCore/Sources/MacIntegration/MacIntegrationPermissions.swift:243)). The sampled local copy is not refreshed when returning from the pushed page, so it can drift from permissions changed there. | **MISLEADING** — a display-only navigation affordance is painted as a functioning Toggle ([SetupView.swift:390](Sources/NativeAgentApp/SetupView.swift:390)). |
| Posture picker: Ask / Balanced / Trusted / Everything | `appModel.trustPolicy.permissionLevel`, `autonomyDefault`, active Full-Mac state, `filePolicy.outsideWorkspaceDefault`, with `chatFileAccess` fallback ([SetupView.swift:406](Sources/NativeAgentApp/SetupView.swift:406), [AppModel+WorkshopPolicy.swift:420](Sources/NativeAgentApp/AppModel+WorkshopPolicy.swift:420)). | Ask→`.safe`; Balanced→`.work`; Trusted→`.builder`; Everything→`.fullMac` ([SetupPresentation.swift:74](Sources/NativeAgentApp/SetupPresentation.swift:74)). `applyPosture` calls `TrustPolicyPresetAction.apply` ([SetupView.swift:485](Sources/NativeAgentApp/SetupView.swift:485)). | SecurityCenter consumes the saved permission, file, autonomy, developer-mode, and Full-Mac fields for every tool decision ([SecurityCenter.swift:247](Modules/NativeAgentCore/Sources/TrustCenter/SecurityCenter.swift:247)). | **No.** Presets rewrite broad policy fields rather than preserving prior fine-grained values ([TrustCenterView.swift:831](Sources/NativeAgentApp/TrustCenterView.swift:831)). Full Mac, if applied, also replaces confirmation/expiry fields ([NativeClient+TrustPolicyActions.swift:224](Sources/NativeAgentApp/NativeClient+TrustPolicyActions.swift:224)). | Every preset sets `developerMode=false`; presets also alter backups, outside-workspace policy, autonomy, permission level, Mac-control policy, and sometimes remote-iOS access ([TrustCenterView.swift:831](Sources/NativeAgentApp/TrustCenterView.swift:831), [NativeClient+TrustPolicyActions.swift:199](Sources/NativeAgentApp/NativeClient+TrustPolicyActions.swift:199)). Display is an observable `AppModel` snapshot refreshed on entry. **Cancel is correct:** selection is not optimistically changed, and “Not now” has an empty action ([SetupView.swift:430](Sources/NativeAgentApp/SetupView.swift:430), [SetupView.swift:462](Sources/NativeAgentApp/SetupView.swift:462)). | **BROKEN** — the confirmed Everything button calls `applyPosture(.everything)` without `fullMacConfirmed: true` ([SetupView.swift:459](Sources/NativeAgentApp/SetupView.swift:459)); the action therefore returns `.confirmationRequired` and writes nothing ([TrustCenterView.swift:1004](Sources/NativeAgentApp/TrustCenterView.swift:1004)). |
| “Thinks with” chat-mind picker | Local observable copies: `providersList`, `chatProvider`, `chatModel`, `chatReasoningEffort`, and `chatFastMode` ([SetupView.swift:897](Sources/NativeAgentApp/SetupView.swift:897), [SetupView.swift:937](Sources/NativeAgentApp/SetupView.swift:937), [AppModel.swift:132](Sources/NativeAgentApp/AppModel.swift:132), [AppModel.swift:580](Sources/NativeAgentApp/AppModel.swift:580)). | Provider change first calls `setChatProvider` → active-provider store; then assigns model, may alter effort/Fast, and calls `saveChatBrainDefaults` → `configureModel(surface:"chat")` ([SetupView.swift:945](Sources/NativeAgentApp/SetupView.swift:945), [AppModel+ProvidersAuth.swift:338](Sources/NativeAgentApp/AppModel+ProvidersAuth.swift:338), [AppModel+ProvidersAuth.swift:244](Sources/NativeAgentApp/AppModel+ProvidersAuth.swift:244)). | Chat takes one checked routing snapshot, then selects its `chat` preference and active provider ([ChatOrchestration+TurnEngine.swift:1377](Modules/NativeAgentCore/Sources/ChatOrchestration/ChatOrchestration+TurnEngine.swift:1377)); routing reads `providers/surfaces.json` plus `providers/active.json` ([ProviderRouting.swift:1459](Modules/NativeAgentCore/Sources/ProviderRouting/ProviderRouting.swift:1459)). | **No.** Returning to the prior model does not restore the prior effort or Fast setting if the intermediate model forced them off/fallback. A model-save failure can also leave the already-saved provider change in place. | Model choice can silently change reasoning effort and disable Fast ([SetupView.swift:958](Sources/NativeAgentApp/SetupView.swift:958)); successful saves asynchronously publish provider status to iPhone ([AppModel+ProvidersAuth.swift:294](Sources/NativeAgentApp/AppModel+ProvidersAuth.swift:294)). The UI uses local cached fields; Setup reloads the provider pin but does not call `refreshSurfacePickerCache` for the model ([SetupView.swift:296](Sources/NativeAgentApp/SetupView.swift:296), [AppModel+ProvidersAuth.swift:311](Sources/NativeAgentApp/AppModel+ProvidersAuth.swift:311)). | **BROKEN** — provider and model are two separately failing writes even though the routing owner exposes an atomic `configureSurfaceSelection` operation ([NativeClient+ProviderWorkflowGraph.swift:66](Sources/NativeAgentApp/NativeClient+ProviderWorkflowGraph.swift:66)). |
| Appearance | `nativeagent.darkMode` via `@AppStorage` ([SetupView.swift:212](Sources/NativeAgentApp/SetupView.swift:212)). | Direct `@AppStorage` binding ([SetupView.swift:558](Sources/NativeAgentApp/SetupView.swift:558)). | Main and Settings scenes observe the same key and update `AppearanceController`, which applies the resulting AppKit appearance ([NativeAgentApp.swift:190](Sources/NativeAgentApp/NativeAgentApp.swift:190), [NativeAgentApp.swift:247](Sources/NativeAgentApp/NativeAgentApp.swift:247), [AppearanceController.swift:41](Sources/NativeAgentApp/AppearanceController.swift:41)). | **Yes** for the preference. Off correctly follows the live system appearance rather than forcing light ([AppearanceController.swift:27](Sources/NativeAgentApp/AppearanceController.swift:27)). | Changes `NSApp.appearance` process-wide ([AppearanceController.swift:47](Sources/NativeAgentApp/AppearanceController.swift:47)). State is directly store-backed. | **WORKS** ([SetupView.swift:545](Sources/NativeAgentApp/SetupView.swift:545)). |
| Telegram card | `telegramStatus?.tokenConfigured`, falling back to cached `telegramTokenConfigured` ([SetupView.swift:541](Sources/NativeAgentApp/SetupView.swift:541)). | Card writes nothing; `SetupInfoCard` is a `NavigationLink` to `.telegram` ([SetupView.swift:526](Sources/NativeAgentApp/SetupView.swift:526), [SetupView.swift:806](Sources/NativeAgentApp/SetupView.swift:806)). | Route resolves to `TelegramView` ([SetupView.swift:169](Sources/NativeAgentApp/SetupView.swift:169)). Telegram’s actual status includes `enabled`, `pollerEnabled`, and errors as well as token presence ([NativeClient+ProviderTelegramSessions.swift:369](Sources/NativeAgentApp/NativeClient+ProviderTelegramSessions.swift:369)). | Card click/back: **yes**, absent edits on the destination. | “Connected” means only that a nonempty token is stored; it ignores disabled configuration and a stopped poller. Failed Settings refresh retains the old `telegramStatus` ([AppModel+ChatSessions.swift:266](Sources/NativeAgentApp/AppModel+ChatSessions.swift:266)), and this card displays no stale warning. | **MISLEADING** — a saved token is labelled “Connected” even when Telegram is disabled or its poller is not running ([SetupView.swift:530](Sources/NativeAgentApp/SetupView.swift:530)). |
| iPhone card | One-time local `@State peerPaired`, populated from existence of `mobile/signed_peer_evidence.json` ([SetupView.swift:218](Sources/NativeAgentApp/SetupView.swift:218), [SetupView.swift:279](Sources/NativeAgentApp/SetupView.swift:279), [SignedPeerEvidence.swift:22](Sources/NativeAgentApp/SignedPeerEvidence.swift:22)). | Card writes nothing; it routes to `.pairDevice` ([SetupView.swift:533](Sources/NativeAgentApp/SetupView.swift:533)). | Route resolves to `MacPairingView` ([SetupView.swift:173](Sources/NativeAgentApp/SetupView.swift:173)); signed evidence is written only after authenticated peer traffic ([SignedPeerEvidence.swift:37](Sources/NativeAgentApp/SignedPeerEvidence.swift:37)). | On an existing install, navigation itself is read-only. On a fresh install: **no**—opening the destination generates and persists a new pairing secret. | `MacPairingView.task` calls `currentSecretBase64` ([MacPairingView.swift:198](Sources/NativeAgentApp/MacPairingView.swift:198)), which calls `loadOrGenerateSecret` ([PairingSecretManager.swift:62](Sources/NativeAgentApp/PairingSecretManager.swift:62)). `peerPaired` has no invalidation/subscription, so it can remain stale while Setup stays mounted. | **MISLEADING** — status is a sampled local copy, and merely opening the routed page can create durable credential state ([MacPairingView.swift:198](Sources/NativeAgentApp/MacPairingView.swift:198)). |
| “Inner life and memory” section label | Static literal ([SetupView.swift:253](Sources/NativeAgentApp/SetupView.swift:253)). | None. | `sectionLabel` renders uppercase text only ([SetupView.swift:304](Sources/NativeAgentApp/SetupView.swift:304)). | N/A. | Display-only, honestly so. | **WORKS**. |
| “And the rest” section label | Static literal ([SetupView.swift:255](Sources/NativeAgentApp/SetupView.swift:255)). | None. | `sectionLabel` renders uppercase text only ([SetupView.swift:304](Sources/NativeAgentApp/SetupView.swift:304)). | N/A. | Display-only, honestly so. | **WORKS**. |

## Ranked defects

1. **Everything confirmation is a no-op.** Pass `fullMacConfirmed: true` from the destructive dialog button into `TrustPolicyPresetAction.apply`, or expose a confirmed `applyPosture` variant that cannot accidentally re-enter the confirmation transition.

2. **The chat-mind picker performs provider and model changes as two separate transactions.** Replace `setChatProvider` followed by `saveChatBrainDefaults` with the existing atomic `configureSurfaceSelection(surface:providerID:model:reasoningEffort:serviceTier:)`, and roll all observable fields back from its canonical read on failure.

3. **“Use my Mac” is a fake switch.** Replace it with an explicit disclosure/navigation affordance, or implement a real aggregate enable/disable operation; in either case read with `currentChecked()` and refresh on permission-store invalidation or return navigation.

4. **“An inner life” claims control over memory-in-every-reply but off leaves Context Flow active.** Either remove that claim and limit the action to cognition, or define and implement a symmetric aggregate transaction that snapshots/restores subordinate cognition and Context Flow choices.

5. **Trust posture presets silently rewrite settings outside the picker’s stated meaning.** Patch only the authority fields represented by the posture, or disclose that each choice resets developer mode, backup policy, Mac-control policy, remote-iOS permission, and related trust fields.

6. **Telegram calls a stored token “Connected.”** Change the label to “Token saved,” or require live `enabled && pollerEnabled` evidence for “Connected” and expose stale-refresh state on this card.

7. **The iPhone status is a one-shot local sample and opening its route can create a secret.** Subscribe Setup to signed-peer evidence changes, reread on return, and generate pairing material only from an explicit setup action.

8. **The Settings header can retain a cached persona name.** Refresh the canonical personality projection when Settings mounts or subscribe the header to the persona-owner invalidation.