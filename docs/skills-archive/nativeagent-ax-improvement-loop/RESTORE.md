# AX loop backup

Snapshot saved on 2026-09-15 alongside the NativeAgent work it guided.
The working skill lives at `~/.agents/skills/nativeagent-ax-improvement-loop`.
To restore it on another Mac, copy this directory's `SKILL.md`, `references/`
and `agents/` into that location. Adjust the repository path in `SKILL.md` if
the checkout lives elsewhere. This archive does not start a campaign.

The installed skill remains the working copy; refresh this snapshot when
backing up future loop changes. Runtime state and credentials are not included.
