
## Text size limitation (unresolved)

The `*-1024-*` frames render at 1024×700 with the largest Dynamic Type environment set, but the
page's ShellType fonts are fixed-size, so the text does not grow. These frames prove the page fits a
small window; they are not a large-text accessibility pass. The limitation is shared with the rail.
